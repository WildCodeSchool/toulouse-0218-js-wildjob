Mission 1
a) coté frontend app.js
  - ds page admin, ajout eventlistener pour détecter soumission form admin
  - puis récupération données via champ 'input, textarea, select...'
    (! Tous ces champs doivent avoir un attribut 'name'),
    le form doit avoir un attribut 'action' dans lequel on met un URL.
  - puis requete fetch, méthode post vers le server. 
    Ici utiliser fetch ac un 2e parametre, un objet avec méthode POST (cf vidéo + code source Todo app)

b) gestion de cette requete coté server, app.post pour récupérer url et le lier à fonction ex :
  app.get('*', (req, res) => {
  res.send(indexHtml)
  })


1/ Si tu merges vers gather et que ça se passe mal.... ANNULER un merge
git merge --abort

2/ Depuis gather, crée branche merge-gather-backend
git merge refactor-backend
